<div class="list-wrap">
<% @lists.each do |list| %>
  <div class="list-box">
    <% if list.image.attached? %>
      <%= image_tag list.image, size: "200x200" %>
    <% else %>
      <%= image_tag 'no_image', size: "200x200" %>
    <% end %>
    <div class="list-comment">
      <span class="list-body"><%= list.body %></span>
      <span class="list-title"><%=link_to list.title, list_path(list.id) %></span>
    </div>
  </div>
<% end %>
</div>

<!--<h1>投稿一覧</h1>  11章で下記消してます-->

<!--% @lists.each do |list| %> <!--list.eachでListレコードの数だけ繰り返し処理を行う-->

<!--  <p>タイトル</p>-->
<!--  <span>-->
<!--    %= link_to list.title, list_path(list.id) %> <!--list.titleと記述することで投稿のタイトルを表示-->
                                                  <!--link_toでindexからshow(詳細画面)へのリンクを作る-->
<!--  </span>-->

<!--  <p>画像</p>-->
    <!--% if list.image.attached? %> <!--list.imageに対し.attached?というimageが存在するかを-->
                                  <!--確認するメソッドを呼び出し-->
    <!--  %= image_tag list.image, size: "200x200" %>-->
    <!--% else %>-->
    <!--  %= image_tag 'no_image', size: "200x200" %>-->
    <!--% end %-->

<!--    %= image_tag list.image, size: "200x200" %> <!--8章で追加-->


<!--% end %>-->